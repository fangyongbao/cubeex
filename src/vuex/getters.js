// 获取城市联动状态
export const getAreaStatus = state => state.global.areaStatus;

// 获取视图切换方向
export const getDirection = state => state.global.direction;

// 获取规则弹出框状态
export const getRuleStatus = state => state.game.ruleStatus;

// 获取商品列表
export const getShopList = state => state.shop.shopList;
